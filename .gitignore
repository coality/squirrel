# Runtime state and logs (never versioned)
logs/
state/
run.lock

# Real configuration (only the *.example files are versioned)
squirrel.conf
targets.tsv
*.local.conf

# Test scratch
tests/tmp/
*.tmp
